# 저장소 루트가 배포 디렉터리라 정적 자산과 함께 올라가지만, 웹에 공개할 의도가 없는
# 내부 파일들(DB 스키마, 마이그레이션, 프로젝트 설명 문서)이 그대로 서빙되고 있었다.
# Pages _redirects는 커스텀 404 상태를 지원하지 않아, 200 rewrite로 홈 화면을 대신
# 내려주는 방식으로 실제 파일 노출을 막는다 (매칭되지 않는 다른 경로들과 동일한 동작).
/schema.sql / 200
/CLAUDE.md / 200
/migrations/* / 200
/backup-*.sql / 200
