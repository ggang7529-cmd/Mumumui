// 추천을 보여주기 시작하는 최소 별점 개수.
//
// 브라우저(js/render.js)와 서버(functions/api/recommend/[name].js)가 같은 값을 써야 해서
// js/moodTags.js·js/kdc.js처럼 한 곳에 둔다. 서버는 이 개수 미만이면 계산 자체를 건너뛰고,
// 화면은 "지금 1개 · 2개만 더" 식으로 남은 개수를 보여준다.
//
// 1개로 뽑으면 근거가 하나뿐이라 "그 책과 같은 작가/분류 목록"이 그대로 나와서 추천이
// 아니라 검색처럼 읽힌다. 3개쯤 되면 서로 다른 근거가 섞여 순서가 납득된다. 5개 이상은
// 지금 규모(책 67권)에서 대부분의 사람이 도달하지 못해 기능이 잠겨버린다.
export var MIN_RATINGS_FOR_RECOMMEND = 3;
